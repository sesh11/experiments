anthropic>=0.40.0
# OpenRouter uses its OpenAI-compatible Chat Completions endpoint.
openai>=2.0.0
datasets>=2.14.0
matplotlib>=3.7.0
pytest>=7.0.0
# Official SWE-bench harness — authoritative Docker scoring (--backend docker).
# 5.x drops the run_evaluation flags the docker backend passes.
swebench>=4.0.0,<5
# Stirrup agent runtime (baseline-stirrup variant). fastapi/orjson are litellm
# runtime requirements it does not declare but imports for tool-schema calls.
stirrup[litellm]==0.1.12
fastapi>=0.115.0
orjson>=3.10.0
